# =====================================================================
# Python
# =====================================================================
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/

# =====================================================================
# C++ / CMake
# =====================================================================
build/
cmake-build-*/
*.o
*.obj
*.so
*.dylib
*.dll
*.exe
*.a
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# =====================================================================
# LaTeX (auxiliary files; the .tex source is tracked, the .pdf is not)
# =====================================================================
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.run.xml
*.bcf
*.pdf

# =====================================================================
# Editors / OS
# =====================================================================
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
*.save
# Phase 3 leftover debug scripts (kept locally, not versioned)
python/preflight_phase3.py
python/probe_signatures.py

# Whitelist: writeup PDFs under theory/ are tracked despite the *.pdf rule above.
# Without this, a future `git rm --cached '*.pdf'` would accidentally untrack them.
!theory/**/*.pdf
